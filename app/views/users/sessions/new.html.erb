<div class="container">
  <div class="row">
    <h2>会員の方はこちらからログイン</h2>
  </div>

  <div class="row center-block">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "form-horizontal" }) do |f| %>
        <div class="form-group">
          <div class="col-xs-2">
            <%= f.label "メールアドレス" %>
          </div>
          <div class="col-xs-10">
            <%= f.email_field :email, autocomplete: "email", :size => "60*60" %>
          </div>
        </div>

        <div class="form-group">
          <div class="col-xs-2">
            <%= f.label "パスワード" %>
          </div>
          <div class="col-xs-10">
            <%= f.password_field :password, autocomplete: "current-password", :size => "60*60" %>
          </div>
          <div class="col-xs-3 col-xs-offset-4">
            <%= render "users/shared/links" %>
          </div>
        </div>
        <div class="form-group">
          <%= f.submit "ログイン", class: "col-xs-2 col-xs-offset-3 btn btn-primary" %>
        </div>
    <% end %>
  </div>

  <div class="row">
    <h2>登録がお済みでない方</h2>
  </div>

  <div class="row">
    <h4><%= link_to  "こちら", new_user_registration_path %>   から新規登録行ってください。</h4>
  </div>

  
</div>
